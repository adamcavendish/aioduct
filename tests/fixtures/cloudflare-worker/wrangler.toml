name = "aioduct-cloudflare-worker-fixture"
main = "build/worker/shim.mjs"
compatibility_date = "2026-07-29"

[build]
# This fixture tests worker-rs 0.8.5 independently of worker-build 0.8.5's
# forced abort-handler packaging path.
command = "cargo install --quiet --locked --version 0.8.4 worker-build && worker-build --release"
