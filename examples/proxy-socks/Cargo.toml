[package]
name = "example-proxy-socks"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
aioduct = { path = "../../crates/aioduct", features = ["tokio", "rustls"] }
tokio = { version = "1", features = ["full"] }
