[package]
name = "example-proxy-socks"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
aioduct = { workspace = true, features = ["tokio", "rustls"] }
tokio = { version = "1", features = ["full"] }
