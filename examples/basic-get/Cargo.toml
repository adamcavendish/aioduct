[package]
name = "example-basic-get"
version.workspace = true
edition.workspace = true
publish = false

[dependencies]
aioduct = { workspace = true, features = ["tokio"] }
tokio = { version = "1", features = ["full"] }
